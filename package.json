{
  "name": "website",
  "type": "module",
  "version": "0.5.0",
  "scripts": {
    "dev": "concurrently --kill-others \"tinacms dev\" \"astro dev\" \"firebase emulators:start --only auth,firestore\"",
    "start": "astro dev",
    "build": "astro build && node utils/cleanup",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^2.3.1",
    "@tinacms/cli": "^1.4.1",
    "astro": "^2.1.5",
    "astro-spa": "^1.3.9",
    "firebase": "^9.17.2",
    "tinacms": "^1.4.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "sass": "^1.59.2"
  }
}
