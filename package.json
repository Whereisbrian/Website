{
  "name": "website",
  "type": "module",
  "version": "0.3.0",
  "scripts": {
    "dev": "concurrently --kill-others \"astro dev\" \"firebase emulators:start --only auth,firestore\"",
    "start": "astro dev",
    "build": "astro build && node utils/cleanup",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^2.0.18",
    "astro-spa": "^1.3.9",
    "firebase": "^9.17.2"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "sass": "^1.59.2"
  }
}
